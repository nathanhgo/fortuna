/**
 * Fonte única da verdade para as cores de Fortuna.
 * Valores confirmados em architecture_docs/visual.md — não duplicar esses hex em
 * nenhum componente; sempre importar daqui ou ler via `theme.palette`.
 */
export const fortunaColors = {
  gold: '#C9A227',
  graphite: '#1A1A1A',
  ivory: '#F5F1E8',
  wine: '#7A2E38',
  olive: '#74804B',
} as const;
